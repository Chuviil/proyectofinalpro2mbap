import { StyleSheet, View } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

const VotingScreen = () => {
  return (<View></View>);
};

export default VotingScreen;